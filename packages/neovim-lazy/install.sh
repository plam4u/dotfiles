#!/usr/bin/env bash

# Initial setup only.
# We already did that and are tracking the setup.
#
# git clone https://github.com/LazyVim/starter ~/dotfiles/packages/neovim-lazy/stow/.config/neovim-lazy/
# rm -rf ~/dotfiles/packages/neovim-lazy/stow/.config/neovim-lazy/.git
