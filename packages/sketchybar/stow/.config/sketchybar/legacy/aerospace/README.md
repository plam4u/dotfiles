# Legacy AeroSpace integration

This directory preserves the previous AeroSpace-backed workspace integration.
It is not loaded by the active SketchyBar configuration. The status scripts
used by that configuration remain in `../../plugins/` because the new bar also
uses them.

The original active configuration is retained as `sketchybarrc`, and its two
AeroSpace-specific plugins are under `plugins/`.
